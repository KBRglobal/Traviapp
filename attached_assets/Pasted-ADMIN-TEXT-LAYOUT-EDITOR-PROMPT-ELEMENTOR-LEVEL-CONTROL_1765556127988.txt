ADMIN TEXT & LAYOUT EDITOR PROMPT – ELEMENTOR-LEVEL CONTROL (NO CODE)

You are building a VISUAL CONTENT EDITOR inside the CMS.
The editor must behave like a hybrid of Word / Notion / Elementor, while STRICTLY preserving the predefined design system.

CORE PRINCIPLE:
Editors can freely edit content visually, BUT CANNOT break layout, spacing, hierarchy, or brand design rules.

────────────────────────────────
EDITOR MODE – GENERAL BEHAVIOR
────────────────────────────────
- WYSIWYG visual editor (true preview, not markdown-only)
- What the admin sees = what the user sees on frontend
- Inline editing directly on the page
- Section-based editing (no free canvas chaos)

Editing modes per section:
- Text edit
- Image edit
- CTA edit
- SEO/meta edit
- Schema edit (advanced tab)

Undo / redo supported
Autosave draft
Version history (soft requirement)

────────────────────────────────
DESIGN GUARDRAILS (NON-NEGOTIABLE)
────────────────────────────────
- Layouts are LOCKED
- Admin cannot:
  - Change column structure
  - Break grid
  - Move sections out of order
  - Change spacing tokens manually
- Admin CAN:
  - Edit content inside allowed fields
  - Toggle visibility of optional sections
  - Reorder allowed repeatable blocks (FAQ items, cards, tips)

Design tokens enforced:
- Font sizes
- Line height
- Colors
- Button styles
- Border radius
- Shadows
- Grid spacing

If admin attempts forbidden action → silently blocked or snapped back

────────────────────────────────
TEXT EDITOR (WORD-LIKE EXPERIENCE)
────────────────────────────────
Supported inline formatting:
- Bold
- Italic
- Underline
- Links (internal / external / affiliate)
- Bullet list
- Numbered list

Heading rules:
- H1: locked (only editable text)
- H2/H3: selectable only where allowed
- No arbitrary heading insertion

No font picker
No color picker
No custom CSS

Text always inherits design system styles

────────────────────────────────
SECTION-AWARE EDITING
────────────────────────────────
Each section has:
- Editable fields only relevant to that section
- Context-aware toolbar

Examples:
Hero Section:
- Edit H1 text
- Edit subtitle
- Edit CTA text + link
- Replace hero image

FAQ Section:
- Add / remove questions
- Edit question text
- Edit answer text
- Reorder items

Cards / grids:
- Duplicate card
- Remove card (within min/max limits)
- Reorder cards

────────────────────────────────
IMAGE HANDLING – SMART & AUTOMATIC
────────────────────────────────
Every image field has:
- Required aspect ratio
- Max file size
- Required resolution

When admin uploads image:
- System auto:
  - Crops to required ratio
  - Resizes to optimal dimensions
  - Compresses for performance
- No distortion
- No overflow
- No layout break

If image does not meet minimum quality:
- Warning shown
- System attempts auto-enhancement
- Admin can approve or replace

All images automatically generate:
- Filename (SEO)
- ALT text (editable)
- Caption (optional)
- Schema image object

────────────────────────────────
“GENERATE IMAGE” BUTTON (AI IMAGE FLOW)
────────────────────────────────
Each image slot includes:
[ Generate Image ]

On click:
- System reads:
  - Section context
  - Page type (hotel / attraction / article)
  - Heading text
  - Nearby paragraph content
- System auto-generates:
  - Image description
  - Visual style
  - Composition rules
  - Aspect ratio
  - Filename
  - ALT text
- Image is inserted directly into correct slot
- Image respects design constraints automatically

Admin can:
- Regenerate
- Replace
- Edit ALT / caption

No image ever inserted outside its slot

────────────────────────────────
CTA & LINK CONTROL
────────────────────────────────
CTA editor:
- Edit button text
- Select CTA type:
  - Affiliate
  - Internal
  - External
- Affiliate links pulled from affiliate manager
- rel="nofollow sponsored" auto-applied

No raw HTML editing

────────────────────────────────
SEO & META EDITOR (SIDE PANEL)
────────────────────────────────
Dedicated SEO panel per page:
- Meta title
- Meta description
- URL slug
- Primary keyword
- Secondary keywords
- Internal link suggestions

Live SEO validation:
- Character counters
- Warnings for missing fields

────────────────────────────────
SCHEMA EDITOR (ADVANCED TAB)
────────────────────────────────
Schema blocks auto-generated
Admin can:
- View
- Edit values
- Toggle schema on/off
- Add missing fields

No schema code writing required

────────────────────────────────
SAFE EDITING RULES
────────────────────────────────
- Nothing is hardcoded
- Everything editable
- Nothing breaks design
- Nothing publishes without approval
- No content outside system control

────────────────────────────────
FINAL ENFORCEMENT RULE
────────────────────────────────
If the editor allows a user action that can visually break the frontend,
the editor is considered FAILED.

The editor must behave like:
“Maximum freedom inside a protected design cage.”

You must ensure all the above is implemented.
No exceptions.